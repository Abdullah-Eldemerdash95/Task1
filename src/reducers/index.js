import { combineReducers } from 'redux'
import   pics   from './pics'
import categories from './categories'

export default combineReducers({pics, categories})

// we didn't edit on data types we did editing on data inside components